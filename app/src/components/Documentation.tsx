// Deprecated: the in-app Documentation view was replaced by a direct link to
// the published GitBook site (see the "Documentation" nav item in
// Sidebar.tsx, which now points at https://monkey-finance.gitbook.io/monkey-finance-docs/).
// This component is no longer imported or rendered anywhere -- kept as an
// inert stub (rather than a real component) so it doesn't drag in the stale
// `target/idl/pod_vault.json` import that broke the Vercel build. Safe to
// delete this file entirely (`git rm app/src/components/Documentation.tsx`).
export {};
